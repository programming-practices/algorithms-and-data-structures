package functional_programing.a1;

public class Functor {
//    public static <T> T foldLeft(T init, OperatorTwoArguments<T> op, T... array) {
//        T result = init;
//        for (T elem : array) {
//            result = op.apply(result, elem);
//        }
//        return result;
//    }

//    public static <T> T foldLeft(T init, OperatorTwoArguments<T> op, Node<T> tail) {
//        return (tail == null) ? init : op.apply(tail.getValue(), foldLeft(init, op, tail.getNext()));
//    }
}
